# shootinggame


# Inicializamos un entorno virtual
python -m venv venv
venv\Scripts\activate
# Instalamos los requerimeintos 
pip install -r requirements.txt
# Corremos el juego
python shooter_game_main.py
